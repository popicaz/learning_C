= guessing number is negative or positive

#include <stdio.h>

int main() {
    
int num;

printf("enter number: ");
scanf("%d", &num);

if( num > 0)
printf("positive");

else if(num < 0)
printf(" negative");

else
printf("0");

    return 0;
}
